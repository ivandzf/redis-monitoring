import Login from "../../api/login";

const state = {
    
};

const getters = {
    
};

const mutations = {
    
};

const actions = {
    login(context) {
         Login.doLogin()
    }
};

export default {
    state,
    getters,
    mutations,
    actions
};
